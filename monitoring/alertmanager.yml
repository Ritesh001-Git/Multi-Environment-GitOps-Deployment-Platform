route:
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 12h
  receiver: 'null'

receivers:
  - name: 'null'

# To add email alerts later, replace 'null' receiver with:
# receivers:
#   - name: 'email'
#     email_configs:
#       - to: 'you@example.com'
#         from: 'alerts@example.com'
#         smarthost: 'smtp.gmail.com:587'
#         auth_username: 'you@example.com'
#         auth_password: 'your-app-password'
