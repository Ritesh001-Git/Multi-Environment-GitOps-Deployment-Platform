apiVersion: v1
kind: Service
metadata:
  name: gitops-app-service
  namespace: gitops
  labels:
    app: gitops-app
    app.kubernetes.io/name: gitops-app
spec:
  type: ClusterIP
  selector:
    app: gitops-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
